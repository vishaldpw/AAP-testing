---
- hosts: all
  become: yes
  tasks:
  - name: Start vsftpd service
    service:
      name: vsftpd
      state: started

---
- hosts: all
  become: true
  tasks:
    - name: Install cmatrix package
      apt:
       name: cmatrix
       state: present
